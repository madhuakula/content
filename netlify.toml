[build]
  command = "mkdir -p website/public && mv books/* website/public/ && cd website && hugo --gc --minify"
  publish = "website/public"

[build.environment]
  HUGO_VERSION = "0.69.0"
  HUGO_ENABLEGITINFO = "true"

[context.production.environment]
  HUGO_ENV = "production"